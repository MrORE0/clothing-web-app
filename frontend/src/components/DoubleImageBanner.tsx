interface DoubleImageBanner {
    imageSource1: string
   
}

export const DoubleImageBanner: React.FC<DoubleImageBanner> = (imageSource ) => {

    return(
    <>
        {/* ***Loads a single image that consists of two equaly sized images the width of the screen/page*** */}
    </>
    )

}