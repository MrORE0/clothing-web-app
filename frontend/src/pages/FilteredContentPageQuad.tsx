import type { ItemType } from "../types/CardsTypes"

interface FilteredContentInt {
    filteredContent: Array<ItemType>
}

export const FilteredContentPageQuad: React.FC<FilteredContentInt> = (filteredContent) => {

    return (
        <>

            {/* Renders filtered content here on a quad column layout  */}
            {/* ***Needs to have lazy loading mechanism***  */}

        </>
    )

}